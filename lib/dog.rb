class Dog
    def initialize (name, breed="Mutt")
        @name = name #the name argument is stored within a @name instance variable
        @breed = breed #the breed argument is stored within a @breed instance variable
    end
end